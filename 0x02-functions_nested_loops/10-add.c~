#include "main.h"
/**
 *add - adds two integers
 *@a, @b: the aguements for the add function
 *
 *Return: returns the result of the function
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
