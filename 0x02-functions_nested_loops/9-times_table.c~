#include "main.h"
void times_table(void)
{
	int y;
	int x;

	for (y = 0; y < 10; y++)
	{
		for (x = 0; x < 10; x++)
		{
			x *= y;
			_putchar(x + '0');
			_putchar(',');
			_putchar(' ');
			_putchar('\n');
		}
	}
}
