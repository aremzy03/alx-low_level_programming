#include "main.h"
#include <stdlib.h>
/**
 *_abs - prints the absolute number of an integer
 *@num: is the parameter
 *
 *Return: 0
 */
int _abs(int num)
{
	int absolute;

	absolute = abs(num);
	return (absolute);
}
