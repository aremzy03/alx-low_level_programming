#include "main.h"
char *create_array(unsigned int size, char c)
{
	char *arr_ptr;
	unsigned int i;
	if (size == 0)
		return (NULL);
	*arr_ptr = mallock(size);
	for (i = 0, i < size, i++)
	{
		arr_ptr[i] = c;
	}
	return (*arr_ptr);
}		
