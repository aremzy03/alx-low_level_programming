#include "main.h"
#include <string.h>
/**
 *_strlen - returns the length of the string s
 *@s: the arguement for the string
 *
 *Return: the length of string s
 */
int _strlen(char *s)
{
	return (strlen(s));
}
