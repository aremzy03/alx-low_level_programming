#include <stdio.h>
#include <math.h>
int main(void)
{
	long i, primef, num;
	double square;

	num = 612852475143;
	square = sqrt(num);
	for (i = 0; <= square; i++)
	{
		if (num % i == 0)
		{
			primef = num / i;
		}
	}
	printf("%ld\n", primef);
	return (0);
}
