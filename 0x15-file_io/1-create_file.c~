#include "main.h"
#include <fcntl.h>
#include <stdio.h>
int create_file(const char *filename, char *text_content)
{
	int fd;
	if (filename == NULL)
		return STDERR_FILENO;
	fd = fopen(*filename, O_WRONLY | O_CREAT);
	
	
	
	
