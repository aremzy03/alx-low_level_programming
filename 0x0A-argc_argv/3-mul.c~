#include <stdio.h>
int main(int argc, int *argv[])
{
	int i, sum;

	if (argc != 3)
	{
		printf("Error\n");
		return (1);
	}
	for (i = 1; i < argc; i++)
	{
		sum *= argv[i];
	}
	printf("%d", sum);
	return (0);
}
