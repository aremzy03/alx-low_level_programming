#include "main.h"
#include <string.h>
/**
 *_strcmp - compares the two strings
 *@s1: the first string to be compared
 *@s2: the second string to be compared
 *
 *Return: returns the comparative of the two strings
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
